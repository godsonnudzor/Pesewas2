# Example env file (DO NOT commit secrets)
# Copy this to .env and fill values for local development only

SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional client-side (exposed) keys for frontend builds
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
